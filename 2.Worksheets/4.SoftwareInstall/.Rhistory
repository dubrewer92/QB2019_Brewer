print("Hello, world!")
unname()
git --version
system("git --version", intern = TRUE)))
system("git --version", intern = TRUE)
Sys.info()[1:2]
unname(c(Sys.info()[6]
)
)
info <- unname(c(Sys.info()[6],
.Platform$OS.type,
Sys.info()[1:2],
R.version.string,
.Platform$GUI,
system("git --version", intern = TRUE)))
info
